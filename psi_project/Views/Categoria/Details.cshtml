@model psi_project.Models.Categoria

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
    <style>
        .botao {
            display: inline-block;
        }
    </style>
</head>
<body>
    <div>
        <h4>Categoria</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Nome)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.Nome)
            </dd>
    
        </dl>
    </div>
    <p>

        <button class="botao" type="button">@Html.ActionLink("Editar", "Edit", new { id = Model.CategoriaId })</button>
        <button  class="botao" type="button">@Html.ActionLink("Menu principal", "Index")</button>
        
    </p>
</body>
</html>
